### implement a method reverse for number (decimal based 10)

#### instructions:
 * don't convert the number to string or stream of characters
 * don't use library methods
 * treat the input as a stream of digits
